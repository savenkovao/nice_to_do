1. Задавай значения в em, если свойства масштабируются относительно font-size;
(например, если это заголовок, то лучше задать паддинги для него в em)
свойства элемента (кроме font-size) счиаются от font-size элемента

2. Во всех остальных случаях задавай значения в rem.